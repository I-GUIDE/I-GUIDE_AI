node_modules
dist
*.local

# TypeScript incremental build cache — regenerated by every `tsc`, so it showed up as a
# modified file on almost every commit and got swept into unrelated diffs.
*.tsbuildinfo
