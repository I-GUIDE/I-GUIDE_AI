# agent_runtime — Multi-agent orchestration layer
#
# This package owns everything related to agent routing, tool selection,
# executor construction, and streaming.  It depends on ``rag_pipeline``
# for retrieval / generation but never the other way around.
