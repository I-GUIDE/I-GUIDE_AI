# Environment files
embedding-server/.env
rag_pipeline/.env
rag_pipeline/.env.local
opengeodata_prototype/.env
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/
*.egg
.venv/
venv/
ENV/
env/

# Node.js
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# Logs
*.log
wkt_errors.log

# Test results
*_results_*.json
e2e_test_results_*.json
rag_loop_results_*.json

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Generated notebook workflows (runtime artifacts)
MCP_server/generated_notebook_workflows/

# Agent chat runtime artifacts (uploads, generated outputs, metadata, qgis jobs)
agent_chat_files/

# Agent eval / generated output artifacts
outputs/

# Temporary files
*.tmp
*.bak
.env
.env.local
*.env.local
TODO.md

# Fetched third-party sources (scripts/build_method_library.py, measure_source_fetchability.py).
# NEVER commit these: the platform's license-id/license-name fields are empty or null for most
# elements and a spot-checked repo 404s on the GitHub license API, so redistribution rights are
# unclear. The cache is reproducible — resolve_and_fetch records origin_url + sha256 for each.
.corpus_cache/
.source_probe/
