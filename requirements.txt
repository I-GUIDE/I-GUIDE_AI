# Web framework / server
flask
flask-cors
flasgger
gunicorn
fastapi

# HTTP clients
aiohttp
httpx
requests

# LLM / agent stack
openai
langchain>=1.0
langchain-core>=1.0
langchain-openai>=1.0
langgraph>=1.0
langchain-mcp-adapters
mcp[cli]>=1.2

# Search backends
opensearch-py
neo4j
ddgs

# Open-web page extraction (web_fetch): boilerplate removal + HTML -> markdown.
# Present in dev environments already; unpinned they are absent from the Docker image.
beautifulsoup4
lxml
markdownify

# Embeddings / ML
sentence-transformers
transformers
--extra-index-url https://download.pytorch.org/whl/cpu
torch
spacy

# Geospatial
geopandas
shapely
fiona
rasterio
# pyarrow is REQUIRED, not optional — same failure mode as the bs4/lxml note above.
# langchain_geo_tools.py:436 (vector_reproject) and :476 (vector_spatial_join) write
# GeoParquet, and geo_handles.py:42 uses it to pass (Geo)DataFrames between tools.
# It was undeclared, so both tools returned {"ok": false, "error": "Missing optional
# dependency 'pyarrow.parquet'"} in every clean build while passing in dev, which has
# it via anaconda.
pyarrow

# Data / notebooks
pandas
matplotlib
nbformat

# Storage / cloud
boto3

# Config / testing
python-dotenv
pytest
pydantic
